import type { NextConfig } from "next";

// Do not enable `output: "export"` for this app.
// Founder vault notes require server-side access checks, and content-vault/ must never move into public/.
const nextConfig: NextConfig = {};

export default nextConfig;
